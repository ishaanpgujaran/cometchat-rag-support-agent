"""
app/web.py — Web interface stub.

This module will expose the agent as a minimal HTTP endpoint.
Implementation is deferred to a later development phase.
"""

# TODO: implement a minimal web interface (e.g., FastAPI or Flask) that
#       wraps the agent module and returns structured JSON responses.
